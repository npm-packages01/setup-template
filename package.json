{
  "name": "setup-template",
  "version": "0.0.1",
  "description": "",
  "main": "bin/index.js",
  "bin": {
    "setup-template": "bin/index.js"
  },
  "scripts": {
    "build": "npx tsc -b src",
    "watch": "npx tsc -b src -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^8.1.0",
    "typescript": "^3.6.4"
  },
  "devDependencies": {
    "@types/fs-extra": "^8.0.0",
    "@types/node": "^12.7.12"
  }
}
